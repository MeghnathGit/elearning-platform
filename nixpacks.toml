[phases.setup]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
    "mkdir -p static/css static/js static/images templates",
    "echo 'Build completed'"
]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT main:app --preload"